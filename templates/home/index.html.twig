{% extends 'base.html.twig' %}

{% block title %}HomePage{% endblock %}

{% block body %}
    <h1>Welcome to homepage</h1>
    {% if app.user %}
        <p>You currently logged on <strong>{{ app.user.username }}</strong></p>
        <p>Your avatar is : <img src="uploads/avatars/{{ app.user.avatar }}" alt="image of {{ app.user.username }}" width="100" ></p>
    {% endif %}

   {# <div class="bg-body rounded p-4 mt-5">
        {{ form_start(form) }}
        <div>
            {{ form_label(form.image) }}
        </div>
        {{ form_widget(form.image) }}
        <div class="mt-3">
            <button type="submit" class="btn btn-primary">Upload</button>
        </div>
        {{ form_end(form) }}
    </div>#}

{% endblock %}
